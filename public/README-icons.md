# App icons

- **`icon.svg`** — main app icon (512×512 viewBox). Used in the web app manifest for PWA install/splash; no PNG needed.
- **`favicon.svg`** — small variant. The app also uses **`app/icon.svg`** for the browser tab (Next.js picks it up automatically).
